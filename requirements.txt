# Pinned exactly: each Playwright release requires a specific Firefox build,
# and the user's persistent Firefox profiles outlive agent updates. A loose
# range lets two installs of the same agent version resolve to different
# Firefox versions (and downgrades trip Firefox's profile protection).
playwright==1.60.0
httpx>=0.27
apscheduler>=3.10,<4
python-dotenv>=1.0
rich>=13.0
pydantic-settings>=2.0
beautifulsoup4>=4.12
truststore>=0.9
keyring>=25.0
pystray>=0.19
Pillow>=10.0
packaging>=24.0
sentry-sdk>=2.0
# macOS backend for pystray + AppKit (for hide-from-Dock support in tray)
# AppKit is bundled inside pyobjc-framework-Cocoa — no separate package needed
pyobjc-core>=9.2; sys_platform == "darwin"
pyobjc-framework-Cocoa>=9.2; sys_platform == "darwin"
