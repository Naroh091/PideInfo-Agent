name: Build PideInfo Agent

on:
  push:
    branches: [master]
    tags:
      - 'v*'
  pull_request:
    branches: [master]
  workflow_dispatch:
    inputs:
      platforms:
        description: 'Platforms to build (comma-separated: macos,windows,linux or "all")'
        required: false
        default: 'all'
  workflow_call:
    inputs:
      tag_name:
        description: 'Tag name for the release (e.g. v1.0.0)'
        required: true
        type: string
    secrets:
      SENTRY_DSN_AGENT:
        required: false
      MACOS_CERTIFICATE_P12_BASE64:
        required: false
      MACOS_CERTIFICATE_PASSWORD:
        required: false
      MACOS_SIGN_IDENTITY:
        required: false
      APPLE_ID:
        required: false
      APPLE_APP_SPECIFIC_PASSWORD:
        required: false
      APPLE_TEAM_ID:
        required: false

jobs:
  prepare:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.filter.outputs.matrix }}
    steps:
      - name: Build matrix
        id: filter
        run: |
          ALL='[{"os":"macos-14","platform":"macos-arm64","artifact":"PideInfo-Agent-macos-arm64.dmg"},{"os":"windows-latest","platform":"windows-x64","artifact":"PideInfo-Agent-windows-x64-setup.exe"},{"os":"ubuntu-22.04","platform":"linux-x64","artifact":"PideInfo-Agent-linux-x64.AppImage"}]'
          PLATFORMS="${{ inputs.platforms || 'all' }}"
          if [ "$PLATFORMS" = "all" ] || [ -z "$PLATFORMS" ]; then
            echo "matrix={\"include\":$ALL}" >> $GITHUB_OUTPUT
          else
            FILTERED=$(echo "$ALL" | jq -c --arg p "$PLATFORMS" '[.[] | select(.platform as $pl | ($p | split(",") | map(gsub("\\s+";"")) | any(. as $f | $pl | contains($f))))]')
            echo "matrix={\"include\":$FILTERED}" >> $GITHUB_OUTPUT
          fi

  build:
    needs: prepare
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.prepare.outputs.matrix) }}

    runs-on: ${{ matrix.os }}
    name: Build (${{ matrix.platform }})

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Linux system dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libayatana-appindicator3-1 \
            libayatana-appindicator3-dev \
            libgirepository1.0-dev \
            gir1.2-appindicator3-0.1

      - name: Install Python dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller packaging

      - name: Install macOS-specific dependencies
        if: runner.os == 'macOS'
        run: pip install "pyobjc-core>=9.2" "pyobjc-framework-Cocoa>=9.2"

      - name: Install Playwright driver (no browser — downloaded at runtime)
        run: python -c "import playwright; print('Playwright driver ready')"

      - name: Read version from version.py
        id: version
        run: |
          VERSION=$(python -c "from version import __version__; print(__version__)")
          echo "version=$VERSION" >> $GITHUB_OUTPUT
        shell: bash

      - name: Bake Sentry config into the build
        env:
          SENTRY_DSN_AGENT: ${{ secrets.SENTRY_DSN_AGENT }}
        shell: bash
        run: |
          # Tagged release builds or workflow_call releases report as "production";
          # CI / PR / workflow_dispatch builds report as "development" so we can
          # filter them out in Sentry.
          if [[ "${GITHUB_REF}" == refs/tags/v* ]] || [[ -n "${{ inputs.tag_name }}" ]]; then
            ENV_TAG="production"
          else
            ENV_TAG="development"
          fi
          if [[ -z "${SENTRY_DSN_AGENT}" ]]; then
            echo "::warning::SENTRY_DSN_AGENT secret is empty — bundle will ship without telemetry."
          fi
          python - <<PY
          import os
          dsn = os.environ.get("SENTRY_DSN_AGENT", "")
          env = "${ENV_TAG}"
          with open("_baked_env.py", "w", encoding="utf-8") as f:
              f.write("# Generated by .github/workflows/build.yml — do not edit.\n")
              f.write("BAKED_ENV = {\n")
              f.write(f"    \"SENTRY_DSN_AGENT\": {dsn!r},\n")
              f.write(f"    \"SENTRY_ENVIRONMENT\": {env!r},\n")
              f.write("}\n")
          print(f"Wrote _baked_env.py (env={env}, dsn={'set' if dsn else 'empty'})")
          PY

      - name: Build with PyInstaller
        run: pyinstaller build/pideinfo-agent.spec --distpath dist --workpath build/work
        shell: bash

      # -----------------------------------------------------------------------
      # macOS: create DMG
      # -----------------------------------------------------------------------
      - name: Create macOS DMG
        if: runner.os == 'macOS'
        run: |
          APP="dist/PideInfo Agent.app"
          DMG_NAME="${{ matrix.artifact }}"
          hdiutil create \
            -volname "PideInfo Agent" \
            -srcfolder "$APP" \
            -ov \
            -format UDZO \
            "$DMG_NAME"

      # -----------------------------------------------------------------------
      # macOS: code signing (only if secrets are available)
      # -----------------------------------------------------------------------
      - name: Import macOS signing certificate
        if: runner.os == 'macOS' && env.MACOS_CERTIFICATE != ''
        env:
          MACOS_CERTIFICATE: ${{ secrets.MACOS_CERTIFICATE_P12_BASE64 }}
          MACOS_CERTIFICATE_PWD: ${{ secrets.MACOS_CERTIFICATE_PASSWORD }}
        run: |
          echo "$MACOS_CERTIFICATE" | base64 --decode > certificate.p12
          security create-keychain -p "" build.keychain
          security import certificate.p12 -k build.keychain \
            -P "$MACOS_CERTIFICATE_PWD" -T /usr/bin/codesign
          security set-key-partition-list -S apple-tool:,apple: -k "" build.keychain
          security list-keychain -d user -s build.keychain

      - name: Sign macOS app
        if: runner.os == 'macOS' && env.MACOS_SIGN_IDENTITY != ''
        env:
          MACOS_SIGN_IDENTITY: ${{ secrets.MACOS_SIGN_IDENTITY }}
        run: |
          codesign --force --deep --options runtime \
            --entitlements build/macos/entitlements.plist \
            --sign "$MACOS_SIGN_IDENTITY" \
            "dist/PideInfo Agent.app"
          codesign --verify --deep --strict "dist/PideInfo Agent.app"

      - name: Notarize macOS DMG
        if: runner.os == 'macOS' && env.APPLE_ID != ''
        env:
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
          TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: |
          xcrun notarytool submit "${{ matrix.artifact }}" \
            --apple-id "$APPLE_ID" \
            --password "$APP_SPECIFIC_PASSWORD" \
            --team-id "$TEAM_ID" \
            --wait
          xcrun stapler staple "${{ matrix.artifact }}"

      # -----------------------------------------------------------------------
      # Windows: create NSIS installer
      # -----------------------------------------------------------------------
      - name: Install NSIS
        if: runner.os == 'Windows'
        run: |
          choco install nsis --no-progress -y
          echo "C:\Program Files (x86)\NSIS" >> $env:GITHUB_PATH

      - name: Build Windows NSIS installer
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $distDir = Join-Path $pwd "dist\PideInfo Agent"
          $outFile = Join-Path $pwd "${{ matrix.artifact }}"
          makensis /DVERSION=${{ steps.version.outputs.version }} "/DDIST_DIR=$distDir" "/DOUTFILE=$outFile" build\windows\installer.nsi

      # -----------------------------------------------------------------------
      # Linux: create AppImage
      # -----------------------------------------------------------------------
      - name: Generate Linux icon
        if: runner.os == 'Linux'
        run: |
          python - <<'EOF'
          from PIL import Image, ImageDraw
          size = 256
          img = Image.new("RGBA", (size, size), (0, 0, 0, 0))
          draw = ImageDraw.Draw(img)
          m = int(size * 0.06)
          draw.ellipse([m, m, size - m, size - m], fill="#1D4ED8")
          draw.rectangle([size//4, size//6, size//4+size//9, size*5//6], fill="white")
          draw.ellipse([size//4, size//6, size*3//4, size//2], fill="white")
          draw.ellipse([size//4+6, size//6+6, size*3//4-6, size//2-6], fill="#1D4ED8")
          img.save("build/linux/icon.png")
          print("icon.png generated")
          EOF

      - name: Create Linux AppImage
        if: runner.os == 'Linux'
        run: |
          wget -q https://github.com/AppImage/appimagetool/releases/download/continuous/appimagetool-x86_64.AppImage \
            -O appimagetool.AppImage
          chmod +x appimagetool.AppImage

          APPDIR="dist/PideInfo-Agent.AppDir"
          mkdir -p "$APPDIR/usr/bin"

          cp -r "dist/PideInfo Agent/"* "$APPDIR/usr/bin/"

          cp build/linux/pideinfo-agent.desktop "$APPDIR/"
          cp build/linux/icon.png "$APPDIR/pideinfo-agent.png"
          cp build/linux/AppRun "$APPDIR/AppRun"
          chmod +x "$APPDIR/AppRun"

          ARCH=x86_64 ./appimagetool.AppImage --appimage-extract-and-run "$APPDIR" "${{ matrix.artifact }}"

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: ${{ matrix.artifact }}
          retention-days: 30

  # ---------------------------------------------------------------------------
  # Release: only on tag v*. release-please created the GitHub Release already;
  # we just upload the platform binaries to it.
  # ---------------------------------------------------------------------------
  release:
    needs: build
    if: startsWith(github.ref, 'refs/tags/v') || inputs.tag_name
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts/

      - name: Collect release assets
        run: |
          mkdir -p release-assets
          find artifacts/ -type f -exec cp {} release-assets/ \;
          echo "Collected assets:"
          ls -lh release-assets/

      - name: Upload assets to GitHub release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}
        run: |
          TAG="${{ inputs.tag_name || github.ref_name }}"
          gh release upload "$TAG" release-assets/* --clobber
